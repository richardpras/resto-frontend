import OutletPaymentMethodConfigSettings from "@/pages/settings/OutletPaymentMethodConfigSettings";

/** Outlet-scoped payment enablement, static QRIS, and instructions (UI wrapper). */
export default function OutletPaymentSettingsPanel() {
  return <OutletPaymentMethodConfigSettings />;
}
