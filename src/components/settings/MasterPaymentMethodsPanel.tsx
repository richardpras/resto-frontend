import PaymentMethodSettings from "@/pages/settings/PaymentMethodSettings";

/** Global/master payment method catalog CRUD (UI wrapper). */
export default function MasterPaymentMethodsPanel() {
  return <PaymentMethodSettings />;
}
